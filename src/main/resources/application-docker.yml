db:
  config:
    username: devuser
    password: devpass
    url: jdbc:postgresql://postgres:5432/devconnect
spring:
  jpa:
    hibernate:
      ddl-auto: update
app:
  kafka:
    server: kafka:9092
    client_id: devconnect-service
    acks: all
    retries: 3
    lag: 0